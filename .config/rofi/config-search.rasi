@import "~/.config/rofi/config.rasi"  

/* ---- Window ---- */
window {
	width: 40%;
	//orientation: horizontal;
	height: inherit;
    y-offset: 10px;
    location: center;
    children: [ "entry", "message" ];
    border: 2px;
    border-color: white/25%;
}

/* ---- Entry ---- */ 
entry {
	expand: true;
	placeholder: " 󰍉 DDG Search";
	horizontal-align: 0.5;
	padding: 15px;
	border-radius: inherit;
    background-color: rgba(0,0,0,0.7);
    text-color: #e0def4;
}
